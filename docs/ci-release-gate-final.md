# Enterprise CI release gate

This file exists to verify the repository's blocking CI path on a clean branch created from `main`.

The release gate must complete all blocking GitHub Actions jobs successfully before merge.
