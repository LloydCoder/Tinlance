import { SectionHeading } from "../../components/section-heading";

export default function ContactPage() {
  return <section className="section"><div className="container"><SectionHeading eyebrow="Contact" title="Start with the system, not the sales pitch." description="Share the problem, the constraints, and what success looks like. We will use that context to determine the right engineering engagement." /></div></section>;
}
