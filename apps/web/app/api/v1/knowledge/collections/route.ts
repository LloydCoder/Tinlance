import { z } from "zod";
import { authenticateApi, ok, problem } from "@/lib/api/v1";
import { getRequestId } from "@/lib/security/request-id";
import { createCollection } from "@/lib/knowledge";
const schema=z.object({name:z.string().trim().min(1).max(200),description:z.string().trim().max(2000).optional(),visibility:z.enum(["PUBLIC","PLATFORM_INTERNAL","ORGANIZATION_PRIVATE","TEAM_RESTRICTED","PROJECT_RESTRICTED","USER_PRIVATE"]),classification:z.enum(["PUBLIC","INTERNAL","CONFIDENTIAL","RESTRICTED","SECRET"]),scopeType:z.string().trim().max(40).default("ORGANIZATION"),projectId:z.string().trim().max(200).optional(),assessmentId:z.string().trim().max(200).optional()});
export async function POST(request:Request){const auth=await authenticateApi(request);if("response" in auth)return auth.response;const requestId=getRequestId(request);const parsed=schema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return problem(requestId,400,"invalid_knowledge_collection","Invalid knowledge collection",parsed.error.message);try{return ok(request,await createCollection({principalId:auth.principal.userId,userId:auth.principal.userId,principalType:"HUMAN",organizationId:auth.principal.organizationId,requestId,...parsed.data}),201)}catch(e){return problem(requestId,403,"knowledge_collection_denied","Knowledge collection creation denied",e instanceof Error?e.message:"unknown")}}
