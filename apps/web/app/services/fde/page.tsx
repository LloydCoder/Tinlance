import { SectionHeading } from "../../../components/section-heading";

export default function FDEPage() {
  return <section className="section"><div className="container"><SectionHeading eyebrow="Forward Deployed Engineering" title="Put senior engineering where the problem actually lives." description="We work alongside product and operations teams to understand the environment, build the right system, and leave behind production capability rather than a handoff deck." /></div></section>;
}
