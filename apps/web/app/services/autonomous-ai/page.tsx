import { SectionHeading } from "../../../components/section-heading";

export default function AutonomousAIPage() {
  return <section className="section"><div className="container"><SectionHeading eyebrow="Autonomous AI" title="Turn repetitive operations into dependable systems." description="We design AI workflows that connect models, tools, business rules, human review, and observability instead of treating a model response as the product." /></div></section>;
}
