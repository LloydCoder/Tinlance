import { SectionHeading } from "../../../components/section-heading";

export default function AutomationPage() {
  return <section className="section"><div className="container"><SectionHeading eyebrow="Automation" title="Remove operational drag without removing control." description="We map workflows, identify the right automation boundary, and connect systems so teams spend less time moving information between tools." /></div></section>;
}
