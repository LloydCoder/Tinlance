import { Prisma } from "@prisma/client";
import { db } from "@/lib/db";
import { buildPrincipal, enforceSecurity } from "@/lib/security-gateway";
import { hasWorkspacePermission } from "@/lib/workspace/permissions";
export async function publishKnowledge(input:{documentId:string;organizationId:string;userId:string;requestId:string}){const member=await db.$queryRaw<Array<{role:string;globalRole:string}>>(Prisma.sql`SELECT m.role,u.role AS "globalRole" FROM "Member" m JOIN "User" u ON u.id=m."userId" WHERE m."organizationId"=${input.organizationId} AND m."userId"=${input.userId} LIMIT 1`);if(!member[0]||!hasWorkspacePermission({memberRole:member[0].role,isPrivileged:["admin","super-admin"].includes(member[0].globalRole)},"knowledge:publish"))throw new Error("knowledge_publish_denied");const m7=await enforceSecurity({principal:buildPrincipal({principalId:input.userId,principalType:"HUMAN",organizationId:input.organizationId,userId:input.userId}),action:"knowledge.publish",resourceType:"KnowledgeDocument",resourceId:input.documentId,requestedRisk:"MEDIUM",context:{tenantId:input.organizationId,requiredPermission:"knowledge:publish"},requestId:input.requestId});if(m7.decision!=="ALLOW")throw new Error(`knowledge_publish_denied:${m7.reasonCode}`);await db.$transaction(async tx=>{await tx.$executeRaw(Prisma.sql`UPDATE "KnowledgeDocument" SET status='ACTIVE',"publishedAt"=CURRENT_TIMESTAMP,"updatedAt"=CURRENT_TIMESTAMP WHERE id=${input.documentId} AND "organizationId"=${input.organizationId} AND status IN ('QUARANTINED','PROCESSING','INDEXING')`);await tx.$executeRaw(Prisma.sql`UPDATE "KnowledgeDocumentVersion" SET status='ACTIVE',"reviewStatus"='APPROVED' WHERE "documentId"=${input.documentId} AND "organizationId"=${input.organizationId} AND status='INDEXING'`);await tx.$executeRaw(Prisma.sql`UPDATE "KnowledgeChunk" SET status='ACTIVE' WHERE "documentId"=${input.documentId} AND "organizationId"=${input.organizationId} AND status='REVOKED'`)});}
export async function revokeKnowledge(input:{documentId:string;organizationId:string;userId:string;requestId:string}){const member=await db.$queryRaw<Array<{role:string;globalRole:string}>>(Prisma.sql`SELECT m.role,u.role AS "globalRole" FROM "Member" m JOIN "User" u ON u.id=m."userId" WHERE m."organizationId"=${input.organizationId} AND m."userId"=${input.userId} LIMIT 1`);if(!member[0]||!hasWorkspacePermission({memberRole:member[0].role,isPrivileged:["admin","super-admin"].includes(member[0].globalRole)},"knowledge:delete"))throw new Error("knowledge_delete_denied");await db.$transaction(async tx=>{await tx.$executeRaw(Prisma.sql`UPDATE "KnowledgeDocument" SET status='REVOKED',"revokedAt"=CURRENT_TIMESTAMP,"updatedAt"=CURRENT_TIMESTAMP WHERE id=${input.documentId} AND "organizationId"=${input.organizationId}`);await tx.$executeRaw(Prisma.sql`UPDATE "KnowledgeChunk" SET status='REVOKED' WHERE "documentId"=${input.documentId} AND "organizationId"=${input.organizationId}`);await tx.$executeRaw(Prisma.sql`UPDATE "KnowledgeDocumentVersion" SET status='REVOKED' WHERE "documentId"=${input.documentId} AND "organizationId"=${input.organizationId}`)});await db.auditEvent.create({data:{organizationId:input.organizationId,actorUserId:input.userId,action:"M10_KNOWLEDGE_REVOKED",resourceType:"KnowledgeDocument",resourceId:input.documentId,requestId:input.requestId,metadata:{securityBoundary:"M10"}}})}
