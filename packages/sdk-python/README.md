# Tinlance Python SDK

Official Python client for the stable Tinlance v1 API.

The client uses the public OpenAPI contract as the integration boundary. API keys are supplied at runtime and are not persisted by the SDK.

See `docs/api/openapi/v1.json` for the canonical API contract.
