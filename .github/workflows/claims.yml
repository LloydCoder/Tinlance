name: Platform claims

on:
  push:
    branches: [main, 'feat/**', 'fix/**', 'chore/**', 'sprint-f/**']
  pull_request:
    branches: [main, 'gate-a-enterprise-identity-v2']
    types: [opened, synchronize, reopened]
  merge_group:
  workflow_dispatch:

env:
  BETTER_AUTH_SECRET: tinlance-gate-a-ci-auth-secret-32-characters-minimum
  BETTER_AUTH_SCIM_CREDENTIAL_HASH_SECRET: tinlance-gate-a-ci-scim-secret-32-characters-minimum

permissions:
  contents: read

concurrency:
  group: claims-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Claim verification
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: apps/web
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          fetch-depth: 0
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
      - uses: pnpm/action-setup@f40ffcd9367d9f12939873eb1018b921a783ffaa
        with:
          version: 10.14.0
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile=true
        working-directory: .
      - name: Verify platform claims
        env:
          BASE_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: pnpm claims:verify
